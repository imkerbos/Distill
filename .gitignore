/.superpowers/

# Local working notes and agent context. These are kept out of the repository
# only by happening to be untracked today, so a `git add -A` on any future
# branch would stage them. Name them explicitly instead.
/CLAUDE.md
/docs/
/需求.md
