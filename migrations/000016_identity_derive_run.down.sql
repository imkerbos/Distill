-- 回滚身份推导的运行记录。
--
-- 丢掉的是**可以重新得到的结论，不是观测**：这张表的每一行都由一次已经落库的
-- 采集运行推出来，回滚之后照着 collection_run 重跑推导即可再得到 —— 与 000012
-- 的区间表同一档，与 000013 的流量表相反（那两张表存的是只在摄入那一刻存在的
-- 观测，relay 不保留历史，回滚等于永久丢失）。
--
-- 代价仍然要说清楚：回滚期间"这次采集的身份推出来了吗"这个问题答不出，
-- 而它答不出的表现与"推导从来没跑过"一样。这是回滚的已知代价，不是遗漏。
DROP TABLE identity_derive_run;
