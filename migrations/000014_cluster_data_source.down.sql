-- 回滚数据来源登记。
--
-- 丢掉的是**登记，不是观测**：与 000013 相反，这一列的内容可以被 up 完整重建
-- —— 演示集群由上面那条 UPDATE 点名恢复，其余一切回到列默认值 COLLECTED。
-- 唯一不可恢复的是有人手工改过的登记（把某个真集群改成 FIXTURE，或反之），
-- 而那种改动本来就没有写入口，也不该有。
--
-- 回滚后，代码若仍是新版，读集群会因为缺列而失败 —— 这是对的失败方向：
-- 一个读不出数据来源的进程不该继续回答「这些数字来自哪里」。
ALTER TABLE cluster DROP COLUMN data_source;
