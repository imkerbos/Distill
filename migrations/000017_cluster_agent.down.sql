-- 回滚集群 agent 身份表。
--
-- 丢掉的是**凭据登记，不是观测**：已经推上来的采集结果留在 collection_run
-- 与 observed_* 里，不受影响。
--
-- 回滚之后所有 agent 立刻认证失败 —— 这是对的失败方向：一个读不出 token
-- 登记的平台，不该继续接受任何一次自称有身份的推送（规范 §2 Fail Secure）。
--
-- 不可恢复的是 token 本身：库里存的是哈希，重新 up 之后必须重新签发。这正是
-- 期望行为 —— 回滚一次凭据表等于一次全量轮换。
DROP TABLE cluster_agent;
