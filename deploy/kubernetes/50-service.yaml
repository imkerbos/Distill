apiVersion: v1
kind: Service
metadata:
  name: distill-api
  namespace: distill
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: distill-api}
  ports:
    - {name: http, port: 10100, targetPort: http}
